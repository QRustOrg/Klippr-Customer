# Auth ViewModel

## Proposito

Documenta el futuro ViewModel de autenticacion.

## Responsabilidad en Clean Architecture

Pertenecera a `iam/presentation/viewmodel` y coordinara casos de uso de IAM para exponer estado a las pantallas.

## Sin implementacion Kotlin

Este archivo no contiene implementacion Kotlin real ni ViewModel activo.

## Futuro archivo

Luego podra convertirse en `AuthViewModel`, usando `SignInUseCase`, `SignUpConsumerUseCase`, `SignUpBusinessUseCase` y `GetCurrentUserUseCase`.
